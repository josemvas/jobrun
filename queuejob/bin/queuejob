#!/bin/sh
if test ! -L "$0"; then
    echo 'Este script no se puede ejecutar directamente'
    exit
fi
exec env \
LD_LIBRARY_PATH={libpath} \
PYTHONPATH={moduledir} \
SPECPATH={specdir} \
{python} -m queuejob.main "$(basename "$0")" "$@"
