#!/usr/bin/env python
import sys
assert sys.version_info >= (2, 7)

import os
from job2q import main
cfgdir = os.path.join(os.getcwd(), 'etc')
bindir = os.path.join(os.getcwd(), 'bin')
os.makedirs(cfgdir)
os.makedirs(bindir)
main.setup(cfgdir=cfgdir, bindir=bindir)

