#!/usr/bin/env python
# -*- coding: utf-8 -*-
import os
import sys
if sys.version_info != {version}:
    for var, path in {environ}.items():
        os.environ[var] = path
    os.execve(sys.argv[0], sys.argv, os.environ)
from job2queue import main
main.run('{hostspecs}', '{jobspecs}')
