{
   "clustername": "Miztli",
   "scheduler": "LSF",
   "filesync": "local",
   "logdir": "/tmp",
   "delay": "3",

   "defaults": {
      "queue": "q_htc",
      "scratch": "/tmpu/{group}/{user}"
   },

   "offscript": [
      "test -n \"$TELEGRAM_BOT_URL\" && test -n \"$TELEGRAM_CHAT_ID\" && curl -1 -X POST $TELEGRAM_BOT_URL -d chat_id=$TELEGRAM_CHAT_ID --data-urlencode text=\"Tu cálculo \\\"$jobname\\\" en $clustername ha finalizado\""
   ]
}
