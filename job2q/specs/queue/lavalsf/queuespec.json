{
   "sbmtcmd": [ "bsub" ],
   "statcmd": [ "bjobs", "-ostat", "-noheader" ],
   "jobidex": "<([0-9]+)>",

   "qctrl": {
      "name": "#BSUB -J '{}'",
      "label": "#BSUB -P '{}'",
      "nodes": "#BSUB -m '{}'",
      "ncore": "#BSUB -n '{}'",
      "nhost": "#BSUB -R 'span[hosts={}]'",
      "queue": "#BSUB -q '{}'",
      "output": "#BSUB -o '{}/%J.out'",
      "error": "#BSUB -e '{}/%J.err'"
    },
    
   "qenv": {
      "jobid": "$LSB_JOBID",
      "ncore": "$(echo $LSB_HOSTS | wc -w)",
      "hosts": "$(printf '%s\\n' $LSB_HOSTS | uniq)"
    },
    
   "mpilauncher": {
      "openmpi": "openmpi-mpirun",
      "intelmpi": "impi-mpirun",
      "mpich": "mpich-mpirun"
    },
    
   "blocking_states": {
      "PEND": "El trabajo {name} no se envió porque ya está en cola con número {id}",
      "RUN": "El trabajo {name} no se envió porque ya está corriendo con número {id}",
      "PSUSP": "El trabajo {name} no se envió porque está suspendido con número {id}",
      "USUSP": "El trabajo {name} no se envió porque está suspendido con número {id}",
      "SSUSP": "El trabajo {name} no se envió porque está suspendido con número {id}"
    },
    
   "ready_states": [
      "DONE",
      "EXIT"
    ]
}

