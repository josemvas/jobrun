{
   "sbmtcmd": [ "qsub" ],
   "statcmd": [ "qstat", "?" ],
   "jobidex": "?",

   "qctrl": [
      "#PBS -N '{name}'",
      "#PBS -W label='{label}'",
      "#PBS -l 'nodes={nodes}:ppn={ncore}'",
      "#PBS -l 'nodes=1:ppn={ncore}'",
      "#PBS -l 'nodes={nhost}:ppn={ncore}'",
      "#PBS -q '{queue}'",
      "#PBS -o '{output}/%J.out'",
      "#PBS -e '{error}/%J.err'"
    ],
    
   "qenv": {
      "jobid": "$PBS_JOBID",
      "ncore": "$(echo $PBS_NODEFILE | wc -w)",
      "hosts": "$(printf '%s\\n' $PBS_NODEFILE | uniq)"
    },
    
   "mpilauncher": {
      "openmpi": "mpirun",
      "intelmpi": "mpirun",
      "mpich": "mpirun"
    },
    
   "blocking_states": {
      "Q": "El trabajo \"{{}}\" no se envió porque ya está en cola con jobid {}",
      "R": "El trabajo \"{{}}\" no se envió porque ya está corriendo con jobid {}",
      "H": "El trabajo \"{{}}\" no se envió porque está retenido con jobid {}",
      "S": "El trabajo \"{{}}\" no se envió porque está suspendido con jobid {}",
      "W": "El trabajo \"{{}}\" no se envió porque está esperando a ser ejecutado con jobid {}",
      "T": "El trabajo \"{{}}\" no se envió porque está siendo movido a otro host con jobid {}"
    },
    
   "ready_states": [
      "C",
      "E"
    ]
}

