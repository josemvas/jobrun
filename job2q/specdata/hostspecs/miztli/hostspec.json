{
   "clustername" : "Miztli",
   "headname": "{hostname}",
   "scheduler": "platformlsf",
   "hostcopy": "remote",
   "logdir": "/home/{group}/{user}/.jobs",
   "onscript": [
      "module purge"
   ],
   "defaults": {
      "queue": "q_htc",
      "scrdir": "/scratch",
      "waitime": "3"
   },
   "offscript": [
      "curl -s -X POST '{TELEGRAM_BOT_URL}' -d chat_id={TELEGRAM_CHAT_ID} --data-urlencode text='Tu cálculo \\\"{jobname}\\\" en {clustername} ha finalizado'"
   ]
}
